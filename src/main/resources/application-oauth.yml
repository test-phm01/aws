spring:
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: ${KAKAO_CLIENT_ID}
            client-secret: ${KAKAO_CLIENT_SECRET}
            redirect-uri: http://${EC2_HOST}/login/oauth2/code/kakao
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            scope: profile_nickname, profile_image, account_email

          naver:
            client-id: ${NAVER_CLIENT_ID}
            client-secret: ${NAVER_CLIENT_SECRET}
            redirect-uri: http://${EC2_HOST}/login/oauth2/code/naver
            authorization-grant-type: authorization_code
            scope: name, email, profile_image

        provider:
          kakao:
            authorization_uri: http://kauth.kakao.com/oauth/authorize
            token_uri: http://kauth.kakao.com/oauth/token
            user-info-uri: http://kapi.kakao.com/v2/user/me
            user_name_attribute: id

          naver:
            authorization_uri: http://nid.naver.com/oauth2.0/authorize
            token_uri: http://nid.naver.com/oauth2.0/token
            user-info-uri: http://openapi.naver.com/v1/nid/me
            user_name_attribute: response